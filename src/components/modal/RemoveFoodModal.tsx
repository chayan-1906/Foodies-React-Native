import {Text, View} from 'react-native';
import {RemoveFoodModalProps} from '../../types';

function RemoveFoodModal({food, restaurant, onClose}: RemoveFoodModalProps) {
    return (
        <View>
            <Text>RemoveFoodModal</Text>
        </View>
    );
}

export default RemoveFoodModal;
